## v2.9.1 修复监控会话结束时间 + 断流次数统计

### 修复内容

#### 1. 修复遗留会话结束时间显示为"进行中..."
- **根因**：`init_db()` 中处理遗留 `active` 会话时只改了 `status='interrupted'`，没设置 `end_time` 和 `duration`
- **修复**：`UPDATE` 语句同时设置 `end_time=NOW()` 和 `duration=计算值`
- **效果**：历史遗留的异常退出会话现在会正确显示结束时间

#### 2. 修复正常退出时会话未关闭
- **根因**：`_quit_app()` 中没有调用 `end_monitor_session()`
- **修复**：退出前先调用 `end_monitor_session()`，确保数据库正确记录
- **效果**：右键托盘"退出监控"后，当前会话会正确关闭

#### 3. 新增"断流次数"统计列
- `get_today_sessions()` 增加每个会话时段内的断流事件查询
- 前端表格增加"断流次数"列：
  - **0次** → 绿色显示
  - **>0次** → 红色加粗显示
- **效果**：一眼就能看出哪次开电脑期间断流最严重

### 文件变更

| 文件 | 变更 |
|------|------|
| `cpe_monitor.py` | 修复init_db、增加断流次数查询、前端表格增加列、版本号更新 |
| `cpe_monitor_desktop.py` | _quit_app()中增加end_monitor_session()调用 |

### 安装说明
1. 下载 CPEMonitor.exe
2. 放到任意目录运行
3. 首次运行会自动创建数据库文件
4. 右键托盘图标可设置开机自启

### 从旧版升级
直接覆盖旧版exe即可，数据库文件会自动保留。
